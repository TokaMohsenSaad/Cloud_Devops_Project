apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: clouddevops-app
  namespace: argo
spec:
  ignoreDifferences:
    - group: ""
      kind: Namespace
      name: argo
      jsonPointers:
        - /metadata
  project: default
  source:
    repoURL: https://github.com/TokaMohsenSaad/Cloud_Devops_Project.git
    targetRevision: HEAD
    path: kubernetes
  destination:
    server: https://kubernetes.default.svc
    namespace: ivolve
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true